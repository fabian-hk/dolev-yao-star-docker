{
  "fstar_exe": "fstar.exe",
  "options": [
    "--cache_dir", "cache",
    "--hint_dir", "hints",
    "--use_hints",
    "--record_hints",
    "--cache_checked_modules",
    "--z3version", "4.15.3"
  ],
  "include_dirs": [
    "src/cat-fact-api",
    "src/hands-on-session-cat-fact-api",
    "src/hands-on-session-login",
    "src/potter-api",
    "src/login",

    "${COMPARSE_HOME}/src",
    "${COMPARSE_HOME}/cache",

    "${DY_HOME}/src/core",
    "${DY_HOME}/src/lib",
    "${DY_HOME}/src/lib/comparse",
    "${DY_HOME}/src/lib/event",
    "${DY_HOME}/src/lib/crypto",
    "${DY_HOME}/src/lib/state",
    "${DY_HOME}/src/lib/hpke",
    "${DY_HOME}/src/lib/utils",
    "${DY_HOME}/src/lib/communication",
    "${DY_HOME}/src/lib/label",
    "${DY_HOME}/cache",

    "${DY_WEB}/src",
    "${DY_WEB}/cache"
  ]
}
